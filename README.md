# 🧩 Claude Skills Registry

トピック志向で開発・管理するAgent Skillsリポジトリ。  
プロジェクトやタスクに応じて、必要なスキルだけを選んでインストールできます。

## コンセプト

```
┌─────────────────────────────────────────────────┐
│  Skills Registry (このリポジトリ)                │
│                                                  │
│  skills/                                         │
│    ├── code-review/       ← トピック単位で分割   │
│    ├── git-conventional/                         │
│    ├── test-gen/                                 │
│    └── ...                                       │
│                                                  │
│  profiles/                                       │
│    ├── web-frontend.json  ← 用途別プリセット     │
│    ├── backend-api.json                          │
│    └── ...                                       │
└──────────────┬──────────────────────────────────┘
               │ install
               ▼
┌──────────────────────────┐
│  Target Project          │
│  .claude/skills/         │
│    ├── code-review/      │
│    ├── test-gen/         │
│    └── ...               │
└──────────────────────────┘
```

## 設計原則

- **Single Responsibility**: 1スキル = 1トピック。肥大化させない
- **Composable**: プロファイルで組み合わせ、プロジェクトに合わせてカスタマイズ
- **Progressive Disclosure**: SKILL.md は軽量に、詳細は references/ に分離
- **Portable**: Agent Skills 標準 (agentskills.io) 準拠

## クイックスタート

```bash
# プロファイルを指定してインストール
./scripts/install.sh --profile web-frontend --target /path/to/project

# 個別スキルをインストール
./scripts/install.sh --skill code-review --target /path/to/project

# 複数スキルを指定
./scripts/install.sh --skills "code-review,test-gen,git-conventional" --target /path/to/project

# インストール済みスキルの一覧
./scripts/install.sh --list --target /path/to/project
```

## ディレクトリ構成

```
.
├── skills/                  # スキル本体（トピックごとに1ディレクトリ）
│   └── <skill-name>/
│       ├── SKILL.md         # 必須: フロントマター + 指示
│       ├── scripts/         # 任意: 実行スクリプト
│       ├── references/      # 任意: 参照ドキュメント
│       └── assets/          # 任意: テンプレート等
│
├── profiles/                # 用途別プリセット（スキルの組み合わせ）
│   └── <profile-name>.json
│
├── catalog.json             # スキルカタログ（メタデータ一覧）
│
├── scripts/                 # リポジトリ管理ツール
│   ├── install.sh           # スキルインストーラー
│   ├── new-skill.sh         # 新規スキル雛形生成
│   └── build-catalog.sh     # catalog.json 自動生成
│
├── templates/               # スキル雛形テンプレート
│   └── SKILL.md.template
│
└── README.md
```

## スキルの作り方

```bash
# 雛形を生成
./scripts/new-skill.sh my-new-skill

# skills/my-new-skill/SKILL.md を編集
# テスト → 反復改善

# カタログを更新
./scripts/build-catalog.sh
```

詳しくは [CONTRIBUTING.md](./CONTRIBUTING.md) を参照。

## プロファイルの仕組み

プロファイルは「このプロジェクトにはこのスキル群が必要」を定義する JSON です。

```json
{
  "name": "web-frontend",
  "description": "Webフロントエンド開発向けスキルセット",
  "skills": ["code-review", "test-gen", "css-design", "a11y-check"]
}
```

自分のプロジェクトに合わせたプロファイルを作って、チームで共有できます。

## ライセンス

MIT
